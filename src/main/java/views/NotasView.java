package views;

import com.intellij.uiDesigner.core.GridLayoutManager;
import controls.Control;
import models.View;
import models.entitys.Turma;
import org.jetbrains.annotations.NotNull;

import javax.swing.*;
import java.awt.*;

import static models.View.TURMA_VIEW;

public class NotasView extends JFrame {
    private final Control control;
    private final Turma turma;
    private JPanel panel;

    public NotasView(Control control, @NotNull Turma turma) {
        super("Registro da turma " + turma.getId());
        this.control = control;
        this.turma = turma;
        $$$setupUI$$$();
        this.setDefaultCloseOperation(DISPOSE_ON_CLOSE);
        this.setContentPane(panel);
        this.setSize(300, 300);
    }

    @Override
    public void dispose() {
        control.changeView(TURMA_VIEW);
        super.dispose();
    }

    /**
     * Method generated by IntelliJ IDEA GUI Designer
     * >>> IMPORTANT!! <<<
     * DO NOT edit this method OR call it in your code!
     *
     * @noinspection ALL
     */
    private void $$$setupUI$$$() {
        panel = new JPanel();
        panel.setLayout(new GridLayoutManager(1, 1, new Insets(10, 10, 10, 10), - 1, - 1));
        panel.setBackground(new Color(- 13487566));
    }

    /**
     * @noinspection ALL
     */
    public JComponent $$$getRootComponent$$$() { return panel; }
}
